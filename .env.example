# Public site origin used for canonical URLs, hreflang alternates,
# sitemap entries, and absolute Open Graph / structured-data URLs.
# Must be the canonical host (with scheme, no trailing slash).
# Set this in your hosting provider's environment for production.
NEXT_PUBLIC_SITE_URL=https://www.sarunpats.com
